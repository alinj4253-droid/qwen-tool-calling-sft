# Dependencies for the dedicated conda env `qwen_tool_sft` (Python 3.11).
# The exact verified versions are frozen in requirements.lock.txt.
# Qwen3-4B (dense Qwen3 architecture) runs on the transformers 4.x line;
# transformers 5.x is only required by the upstream Qwen3.5-9B hybrid model.
torch
transformers>=4.51,<5
trl>=0.18
peft>=0.12
accelerate>=1.0
datasets>=3.0
bitsandbytes>=0.43
safetensors
huggingface_hub
modelscope
sentencepiece
protobuf
pyyaml
pytest
