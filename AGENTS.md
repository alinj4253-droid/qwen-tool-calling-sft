# 项目协作规则

## 注意事项

1. 每次改动完成后，都必须创建一个对应的 Git Commit，以便后续追踪和回滚
2. 每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过
3. 所有项目文件只能位于 `/mnt/ssd2/psf/job/qwen-tool-calling-sft`
4. 禁止修改服务器已有 Conda 环境
5. 禁止删除或修改 `/mnt/ssd2/psf/job` 之外任何文件
6. 每次训练前记录 GPU、环境、代码 Commit 和训练参数
7. 任何可能影响其他用户的操作必须停止
8. 禁止将 SSH 密码、API Key、Token、私有凭据提交到 Git
